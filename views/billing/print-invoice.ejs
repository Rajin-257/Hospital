<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice <%= billing.invoiceNumber %></title>
    <style>
        @media print {
            body {
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
            .no-print {
                display: none !important;
            }
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
            color: #333;
        }
        
        .container {
            max-width: 210mm;
            margin: 20px auto;
            background: white;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            padding: 0;
        }
        
        .invoice-header {
            padding: 15px 30px;
            background: linear-gradient(135deg, #3a8dff 0%, #1565C0 100%);
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .invoice-title {
            font-size: 28px;
            font-weight: 700;
            margin: 0;
        }
        
        .hospital-info {
            text-align: right;
            font-size: 14px;
        }
        
        .hospital-name {
            font-size: 20px;
            font-weight: 600;
            margin-bottom: 5px;
        }
        
        .invoice-body {
            padding: 30px;
        }
        
        .invoice-details {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            border-bottom: 1px solid #eee;
            padding-bottom: 20px;
        }
        
        .details-left, .details-right {
            max-width: 45%;
        }
        
        .details-title {
            font-size: 16px;
            font-weight: 600;
            color: #1565C0;
            margin-bottom: 10px;
        }
        
        .details-info {
            margin: 0;
            line-height: 1.6;
        }
        
        .status-badge {
            display: inline-block;
            padding: 4px 10px;
            border-radius: 15px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }
        
        .status-paid {
            background-color: #4CAF50;
            color: white;
        }
        
        .status-partial {
            background-color: #FF9800;
            color: white;
        }
        
        .status-pending {
            background-color: #F44336;
            color: white;
        }
        
        .invoice-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }
        
        .invoice-table th {
            background-color: #f5f5f5;
            border-bottom: 2px solid #ddd;
            padding: 10px;
            text-align: left;
        }
        
        .invoice-table th:last-child, .invoice-table td:last-child {
            text-align: right;
        }
        
        .invoice-table th:nth-child(2), .invoice-table td:nth-child(2) {
            text-align: center;
        }
        
        .invoice-table th:nth-child(3), .invoice-table td:nth-child(3) {
            text-align: right;
        }
        
        .invoice-table td {
            padding: 12px 10px;
            border-bottom: 1px solid #eee;
        }
        
        .invoice-table tr:last-child td {
            border-bottom: none;
        }
        
        .invoice-total {
            width: 100%;
            max-width: 400px;
            margin-left: auto;
            font-size: 14px;
        }
        
        .invoice-total td {
            padding: 8px 10px;
        }
        
        .invoice-total td:first-child {
            text-align: right;
            font-weight: 600;
        }
        
        .invoice-total td:last-child {
            text-align: right;
            width: 120px;
        }
        
        .total-row {
            font-weight: 700;
            font-size: 16px;
            border-top: 2px solid #eee;
            background-color: #f9f9f9;
        }
        
        .discount {
            color: #F44336;
        }
        
        .paid-amount {
            color: #4CAF50;
        }
        
        .balance-due {
            color: #F44336;
        }
        
        .invoice-notes {
            margin-top: 30px;
            padding: 15px;
            background-color: #f9f9f9;
            border-left: 4px solid #1565C0;
        }
        
        .notes-title {
            font-weight: 600;
            margin-bottom: 10px;
            color: #1565C0;
        }
        
        .invoice-footer {
            margin-top: 50px;
            padding: 20px 30px;
            border-top: 1px solid #eee;
            text-align: center;
            font-size: 14px;
            color: #666;
        }
        
        .qr-code {
            margin-top: 20px;
            text-align: center;
        }
        
        .btn-print {
            background-color: #1565C0;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 4px;
            cursor: pointer;
            margin: 20px 0;
            transition: background-color 0.3s;
        }
        
        .btn-print:hover {
            background-color: #0D47A1;
        }
        
        .signature {
            margin-top: 50px;
            display: flex;
            justify-content: space-between;
        }
        
        .signature-block {
            width: 200px;
            text-align: center;
        }
        
        .signature-line {
            margin: 30px 0 10px 0;
            border-top: 1px solid #333;
        }
        
        .btn-container {
            text-align: center;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="invoice-header">
            <div>
                <h1 class="invoice-title">INVOICE</h1>
                <div style="margin-top: 5px; font-size: 14px;">
                    <strong>Invoice #:</strong> <%= billing.invoiceNumber %>
                </div>
            </div>
            <div class="hospital-info">
                <div class="hospital-name">Hospital Management System</div>
                <div>123 Hospital Street</div>
                <div>City, State 12345</div>
                <div>Phone: (123) 456-7890</div>
                <div>Email: info@hospital.com</div>
            </div>
        </div>
        
        <div class="invoice-body">
            <div class="invoice-details">
                <div class="details-left">
                    <div class="details-title">Patient Information</div>
                    <p class="details-info">
                        <strong><%= billing.Patient.firstName %> <%= billing.Patient.lastName %></strong><br>
                        Patient ID: <%= billing.Patient.patientId %><br>
                        Contact: <%= billing.Patient.contactNumber %><br>
                        Email: <%= billing.Patient.email || 'N/A' %>
                    </p>
                </div>
                <div class="details-right">
                    <div class="details-title">Invoice Details</div>
                    <p class="details-info">
                        <strong>Date:</strong> <%= new Date(billing.invoiceDate).toLocaleDateString() %><br>
                        <strong>Status:</strong> 
                        <span class="status-badge <%= billing.paymentStatus === 'paid' ? 'status-paid' : 
                               billing.paymentStatus === 'partial' ? 'status-partial' : 'status-pending' %>">
                            <%= billing.paymentStatus %>
                        </span><br>
                        <% if (billing.paymentMethod) { %>
                            <strong>Payment Method:</strong> <%= billing.paymentMethod.charAt(0).toUpperCase() + billing.paymentMethod.slice(1) %><br>
                            <% if (billing.paymentDate) { %>
                                <strong>Payment Date:</strong> <%= new Date(billing.paymentDate).toLocaleDateString() %><br>
                            <% } %>
                        <% } %>
                        <% if (billing.insuranceDetails && billing.paymentMethod === 'insurance') { %>
                            <strong>Insurance:</strong> <%= billing.insuranceDetails.company %><br>
                            <strong>Policy Number:</strong> <%= billing.insuranceDetails.policyNumber %>
                        <% } %>
                    </p>
                </div>
            </div>
            
            <table class="invoice-table">
                <thead>
                    <tr>
                        <th>Service Description</th>
                        <th>Quantity</th>
                        <th>Unit Price</th>
                        <th>Total</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (billing.services && Array.isArray(billing.services) && billing.services.length > 0) { %>
                        <% billing.services.forEach(service => { %>
                            <tr>
                                <td><%= service.description %></td>
                                <td><%= service.quantity %></td>
                                <td>$<%= parseFloat(service.price).toFixed(2) %></td>
                                <td>$<%= (parseFloat(service.price) * parseInt(service.quantity)).toFixed(2) %></td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="4" style="text-align: center;">No services listed</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
            
            <table class="invoice-total">
                <tr>
                    <td>Subtotal:</td>
                    <td>$<%= billing.totalAmount %></td>
                </tr>
                <% if (billing.discount && billing.discount > 0) { %>
                    <tr class="discount">
                        <td>Discount (<%= billing.discountType === 'percentage' ? billing.discount + '%' : '$' + billing.discount %>):</td>
                        <td>-$<%= (billing.totalAmount - billing.discountedAmount).toFixed(2) %></td>
                    </tr>
                    <tr>
                        <td>Total After Discount:</td>
                        <td>$<%= billing.discountedAmount %></td>
                    </tr>
                <% } %>
                <tr class="paid-amount">
                    <td>Paid Amount:</td>
                    <td>$<%= billing.paidAmount %></td>
                </tr>
                <% if ((billing.discount > 0 ? billing.discountedAmount : billing.totalAmount) - billing.paidAmount > 0) { %>
                    <tr class="balance-due">
                        <td>Balance Due:</td>
                        <td>$<%= ((billing.discount > 0 ? billing.discountedAmount : billing.totalAmount) - billing.paidAmount).toFixed(2) %></td>
                    </tr>
                <% } %>
                <tr class="total-row">
                    <td>Total:</td>
                    <td>$<%= billing.discountedAmount %></td>
                </tr>
            </table>
            
            <% if (billing.notes) { %>
                <div class="invoice-notes">
                    <div class="notes-title">Notes</div>
                    <p><%= billing.notes %></p>
                </div>
            <% } %>
            
            <div class="signature">
                <div class="signature-block">
                    <div class="signature-line"></div>
                    <div>Authorized Signature</div>
                </div>
                <div class="signature-block">
                    <div class="signature-line"></div>
                    <div>Patient Signature</div>
                </div>
            </div>
        </div>
    </div>
    
    <div class="btn-container no-print">
        <button class="btn-print" onclick="window.print()">Print Invoice</button>
        <button class="btn-print" style="background-color: #555;" onclick="window.history.back()">Go Back</button>
    </div>
</body>
</html> 